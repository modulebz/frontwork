import {IProps as IBlockProps, DefaultProps as BlockDefaultProps } from "../block/IProps"
export interface IProps extends IBlockProps{

}
export const DefaultProps:IProps = Object.assign({

},BlockDefaultProps);
